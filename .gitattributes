# Shell scripts must stay LF — CRLF would break the `#!/bin/sh` shebang inside the
# Linux container (`bad interpreter: /bin/sh^M`). Critical for entrypoint.sh, which
# the Dockerfile COPYs and runs as ENTRYPOINT. Enforce LF on every checkout/build host.
*.sh text eol=lf
entrypoint.sh text eol=lf

# 曾经这里有一条 `/requirements.txt export-ignore`：让 GitHub 源码归档只携带带
# hash 的发布锁，避免 2.8.4 及更早的更新器把源约束文字调整误判成依赖变化。该兼容
# 的前提是发布锁内容始终停在 2.8.4 那一版；2.13.4 升级 cryptography（PYSEC-2026-3552）
# 时锁必然变化，前提不再成立，故按其自身规定移除。
# 本 fork 只服务单一自托管实例且早已运行 2.13.x，不存在 ≤2.8.4 的旧更新器，
# 不需要额外的依赖迁移路径。现在归档同时携带 requirements.txt 与发布锁，
# 更新器看到的就是仓库真实状态。
